# Job Application Assistant - Build Plan

Created 2026-08-12. Spec: `AGENTS.md`.

Ends with two Vercel projects live, real data imported, all suites green.

## Decisions taken before planning

| Question   | Answer                                                  |
| ---------- | ------------------------------------------------------- |
| Back-end   | FastAPI, separate `backend/` folder                     |
| Topology   | BFF - browser talks only to Next; Next talks to FastAPI |
| Hosting    | Two Vercel projects, one repo                           |
| Database   | Nothing provisioned yet; Phase 0 creates it             |
| CSV import | Run during the build against a Neon dev branch          |
| Done when  | Deployed, live URL, smoke-tested                        |

## Steps needing your keyboard

Interactive logins cannot be done for you. They are all in Phase 0 except the last deploy.

- `vercel login`
- Neon provisioning via the Vercel marketplace (browser flow)
- Choosing the real `APP_PASSWORD`

Everything else runs unattended.

---

## Phase 0 - Provisioning

No code. Get credentials in place so nothing later blocks on them.

1. `npm i -g vercel@latest`, then **you run** `vercel login`.
2. **You run** `vercel integration add neon` and complete the browser flow. Create a project named
   `job-application-assistant`.
3. In the Neon console create a branch named `dev` off `main`. `dev` is what the build and the test
   suite point at; `main` stays clean until Phase 9.
4. Capture both connection strings. Use the **pooled** endpoint (`-pooler` in the host).
5. Generate secrets: `openssl rand -base64 32` twice, for `AUTH_SECRET` and `BACKEND_API_KEY`.
6. Choose `APP_PASSWORD`.

Success criteria:

- [x] `vercel whoami` returns your account
- [x] Neon project exists with `main` and `dev` branches
- [x] `psql "$DATABASE_URL" -c 'select 1'` succeeds against the `dev` pooled URL
- [x] All five secret values recorded somewhere you can retrieve them
- [x] Nothing secret is in the repo

Phase 0 outcome, 2026-08-13:

- The Neon project holds two branches: `main` (default) and `dev` branched off it. This repo is
  public, so the project id, org id and endpoint hostnames are deliberately not written down here -
  `neonctl projects list` and `neonctl branches list` are the source of truth.
- `AUTH_SECRET`, `BACKEND_API_KEY`, `APP_PASSWORD` live as non-sensitive env vars on the
  `job-application-assistant` Vercel project across all three environments; `vercel env pull` reads
  them back. The `dev` connection string comes from
  `neonctl connection-string dev --project-id <id> --pooled`.
- `psql` is not installed locally; connectivity was verified with `psycopg`, the driver the backend
  uses.
- Carry into later phases: the Neon resource is connected to `job-application-assistant`, so
  `DATABASE_URL` there points at `main`. Phase 1/2 must write the **`dev`** URL into `backend/.env`
  rather than trusting `vercel env pull`, and Phase 9 must keep `DATABASE_URL` off the frontend
  project.

---

## Phase 1 - Repo scaffolding

Structure and ignore rules first, so no generated file is ever accidentally tracked.

1. `git init`.
2. Root `.gitignore` covering both stacks: `node_modules/`, `.next/`, `out/`, `.env*` (with
   `!.env.example`), `__pycache__/`, `*.pyc`, `.venv/`, `.pytest_cache/`, `.ruff_cache/`,
   `playwright-report/`, `test-results/`, `.vercel/`.
3. Create `frontend/` and `backend/` skeletons per the AGENTS.md layout.
4. `backend/.env.example` and `frontend/.env.example` listing every variable with empty values.
5. Minimal root `README.md`: what it is, how to run both services. Nothing more.

Success criteria:

- [x] `git status` shows no `.env`, no `node_modules`, no `__pycache__`
- [x] Both `.env.example` files list every variable from the AGENTS.md tables
- [x] Directory tree matches the AGENTS.md layout exactly
- [x] README fits on one screen and contains no emojis

Phase 1 outcome, 2026-08-13:

- The repo was already `git init`ed on `master` with no commits and a two-line `.gitignore`; the
  plan's rules were merged into it rather than overwriting.
- `candidatures*` was already ignored and was kept that way - the CSV holds personal data. So
  Phase 10's "keep the CSV as the archival record" means keep it on disk, not tracked in git,
  even though the AGENTS.md layout lists it at the root.
- `backend/.env` and `frontend/.env.local` were written from the Phase 0 secrets and are gitignored;
  `git check-ignore` confirms it while `.env.example` stays trackable. `backend/.env` points at the
  **`dev`** branch and was verified with a live `select 1`.
- Only directories were created under `backend/`; the `.py` files land in Phases 2-5 and the Next.js
  app in Phase 6. Git does not track empty directories, so they show up as the phases fill them.
- Carry into Phase 6: `frontend/` already contains `.env.example` and `.env.local`, and
  `create-next-app` refuses to scaffold into a directory holding files it does not recognise. Move
  the two aside first, or scaffold into a temp directory and merge.
- Nothing has been committed - the working tree is entirely untracked, awaiting your go-ahead.

---

## Phase 2 - Back-end foundation

A running FastAPI service with auth and config, before any database work.

1. `backend/pyproject.toml`: `requires-python = ">=3.13"`, `[tool.vercel] entrypoint = "app.main:app"`,
   deps `fastapi`, `pydantic`, `pydantic-settings`, `sqlalchemy`, `psycopg[binary]`, `alembic`;
   dev deps `pytest`, `httpx`, `ruff`.
2. `app/config.py` - `pydantic-settings` reading `DATABASE_URL` and `BACKEND_API_KEY`. Fail loudly at
   startup if either is missing.
3. `app/security.py` - a dependency comparing `X-API-Key` with `compare_digest`, raising 401.
4. `app/main.py` - the FastAPI instance and `GET /health`, which is the one route with no API key.
5. `ruff` config: line length 100, import sorting on.

Success criteria:

- [x] `uv run fastapi dev app/main.py` starts clean
- [x] `GET /health` returns 200 with no header
- [x] A protected probe route returns 401 with no key, 401 with a wrong key, 200 with the right one
- [x] Deleting `BACKEND_API_KEY` from env makes startup fail with a readable message, not a stack trace
- [x] `uv run ruff check .` and `ruff format --check .` pass

Phase 2 outcome, 2026-08-13:

- Resolved versions: fastapi 0.141.1, pydantic 2.13.4, pydantic-settings 2.15.0, sqlalchemy 2.0.52,
  psycopg 3.3.4, pytest 9.1.1, ruff 0.16.2. `uv.lock` is committed.
- The probe route lives in `tests/test_security.py`, not in the shipped app, so the auth dependency
  is covered without leaving a dead route in production. Five cases: no key, wrong key, a
  one-character-short prefix, a non-ASCII key, and the real key.
- `require_api_key` compares `.encode()`d bytes. Header values arrive latin-1 decoded, and
  `compare_digest` raises `TypeError` on a non-ASCII `str`, which would be a 500 rather than a 401.
- `config.py` catches `ValidationError` and re-raises `SystemExit`, so a missing variable prints
  `Missing environment variables: DATABASE_URL, BACKEND_API_KEY. See .env.example.` and exits 1 with
  no traceback. `tests/test_config.py` asserts that in a subprocess, including the absence of
  `Traceback`.
- Tests use `httpx.AsyncClient` over `ASGITransport` with `pytest-asyncio` in auto mode, so Phase 4's
  contract tests need no new machinery.
- The dev group takes `fastapi-cli[standard-no-fastapi-cloud-cli]` rather than putting
  `fastapi[standard]` in the runtime deps: `fastapi dev` works locally while production resolves to
  20 packages, with no uvicorn, sentry-sdk or fastapi-cloud-cli shipped to Vercel.
- No `[build-system]`, so uv treats this as a virtual project and never installs it. `app` is
  importable because `[tool.pytest.ini_options] pythonpath = ["."]` covers pytest and
  `[tool.vercel] entrypoint` covers deployment.
- Carry into Phase 3: `env_file = ".env"` resolves relative to the working directory, so alembic and
  pytest must both be run from `backend/`.

---

## Phase 3 - Data layer

1. `app/db.py` - engine with `NullPool` (serverless: connections cannot be reused), `sessionmaker`,
   and a `get_session` dependency that commits on success and rolls back on exception.
2. `app/models.py` - `Application` and `StatusUpdate` exactly as specced. `uuid` PKs with server-side
   defaults, `rating` as `REAL`, `date` as `Date` not `DateTime`, cascade delete on the relationship
   **and** on the FK constraint.
3. `alembic init`, configured to read `DATABASE_URL` from env rather than `alembic.ini`.
4. First migration: both tables plus the `(application_id, date desc, created_at desc)` index.
5. Apply to the `dev` branch.

Success criteria:

- [x] `alembic upgrade head` succeeds against `dev`
- [x] `alembic downgrade base` then `upgrade head` succeeds - the migration is reversible
- [x] `\d status_updates` shows the composite index with both `DESC` orderings
- [x] Deleting an application row via raw SQL removes its status updates
- [x] Inserting `rating = 4.5` reads back as exactly `4.5`, not `4.5000001` or a string

Phase 3 outcome, 2026-08-14:

- Revision `3c5e515f4928`, autogenerated then reviewed. `dev` is at head.
- `pg_indexes` reports
  `btree (application_id, date DESC, created_at DESC)`, and `pg_get_constraintdef` reports
  `FOREIGN KEY (application_id) REFERENCES applications(id) ON DELETE CASCADE`.
- `rating` is a genuine `real`; 4.5 inserted reads back as the float `4.5`, equal under `==`.
- Cascade verified twice: a raw SQL `delete from applications` took 2 status updates to 0, and an
  ORM `session.delete()` did the same. The relationship carries `cascade="all, delete-orphan"` with
  `passive_deletes=True`, so the ORM defers to the FK rather than loading children to delete them.
- `db.py` rewrites the `postgresql://` prefix to `postgresql+psycopg://`. SQLAlchemy maps the bare
  prefix to psycopg2, which is not installed, so without this every connection fails at import.
- `created_at` carries `server_default=now()`; `updated_at` uses a client-side `default`/`onupdate`
  so the DDL has no DEFAULT clause, matching the AGENTS.md schema. Raw SQL inserts must therefore
  supply `updated_at` themselves.
- `alembic.ini` has no `sqlalchemy.url`; `env.py` imports the engine from `app.db`, so migrations
  always target whatever `DATABASE_URL` points at. `prepend_sys_path = .` makes `app` importable,
  which is why alembic has to be run from `backend/`.
- Migrations are formatted with `ruff check --fix` and `ruff format` after generation - alembic's
  template output does not match the project style.

---

## Phase 4 - API

The derived-status query is the one genuinely tricky piece here.

1. `app/schemas.py` - Pydantic models. `ApplicationListItem` carries `current_status` and
   `last_update_date`; `ApplicationDetail` carries the full timeline. `Status` is a `StrEnum` of the
   six values, so an invalid status is a 422 rather than a bad row.
2. `app/routers/applications.py` - the seven routes from AGENTS.md.
3. The list query: one `select` with a lateral join onto the latest update per application, ordered
   `date DESC, created_at DESC`, with `include_closed=false` filtering out `Rejected`/`Withdrawn` in
   SQL.
4. `POST /applications` opens a transaction and writes the application plus its first status update
   together.
5. `scripts/export_openapi.py` writing `backend/openapi.json`, so type generation never needs a
   running server.

Success criteria:

- [x] All seven routes reachable, all requiring `X-API-Key` except `/health`
- [x] **Same-date tiebreak**: two updates sharing a date, the later `created_at` wins. Tested both
      insertion orders so a coincidentally-correct ordering cannot pass
- [x] The list endpoint issues **one** query regardless of row count, asserted by counting statements
      via a SQLAlchemy event listener - not by eyeballing logs
- [x] A failed status-update insert inside `POST /applications` leaves **no** orphaned application row
- [x] `include_closed=false` excludes exactly the closed statuses; `true` returns everything
- [x] List is ordered most-recently-updated first
- [x] 404 on unknown id for GET, PATCH, DELETE
- [x] `DELETE /applications/{id}` removes its updates
- [x] Rating outside 1-5, or not on a 0.5 step, is rejected with 422
- [x] `pytest` green; contract tests run through `ASGITransport` with no network

Phase 4 outcome, 2026-08-14:

- 45 tests green in about 70 seconds.
- **Schema change, deliberate**: `status_updates.created_at` defaults to `clock_timestamp()`, not
  `now()`. `now()` is the transaction timestamp, so two updates written in one transaction get
  byte-identical values and the same-date tiebreak has nothing left to order by. Phase 5 hits this
  directly - `No après 1E` inserts Interview and Rejected together. Migration `3c5e515f4928` was
  edited in place and both branches rebuilt, since nothing is deployed yet.
- A third Neon branch, `test`, holds the suite. `TEST_DATABASE_URL` is read by `conftest.py` through
  `python-dotenv` rather than being added to `app.config`, so a test-only setting stays out of the
  application's settings object. It is listed in `backend/.env.example`.
- The test engine deliberately does **not** use `NullPool`. With it, per-test Neon connection setup
  made the suite take three minutes; pooling brings it to seventy seconds. `NullPool` remains
  correct for the app, which is serverless, and wrong for a long-lived test process.
- The one-query claim is enforced by a `before_cursor_execute` listener counting SELECTs, run at 1,
  5 and 20 rows.
- The no-orphan claim is enforced by a listener that raises on any `INSERT INTO status_updates`,
  then asserting the applications table is empty.
- `ApplicationPatch` validates the four required fields against explicit nulls. Pydantic skips
  validators on defaults, so the check fires only when the client actually sent `null`, turning what
  would be a 500 from a not-null violation into a 422.
- `Application.current_status` and `.last_update_date` are Python properties over `updates[0]`, which
  the relationship orders `date DESC, created_at DESC`. The list route does **not** use them - that
  would lazy-load per row - and takes the values from the lateral join instead.
- `scripts/export_openapi.py` must run as `uv run python -m scripts.export_openapi`; running it by
  path puts `scripts/` on `sys.path` instead of `backend/`. `openapi.json` is committed.

---

## Phase 5 - CSV import

Parser and its tests first, then run it. The script reports what it could not parse; it never guesses.

1. `scripts/import_csv.py` using the stdlib `csv` module - the file has quoted newlines and splitting
   on `\n` corrupts it.
2. Date normaliser handling every observed format: `7 Jul 2026`, `08 Jul`, `9 July`, `17/Jul`,
   `27/July`, `12/Aug`, `11 Aug`, `30/07`, `11/08`, and lowercase variants. Year absent means 2026.
3. `Etat` mapping: `Contact reçu`→Contacted, `Envoyé`→Applied, `1E`/`2E`→Interview,
   `Refus direct`→Rejected, `No après 1E`→Interview **then** Rejected.
4. Timeline extraction: leading `<date> - <text>` and `<date> <text>` only. Unmatched text stays in
   the comment. Extracted updates inherit the row's `Etat`-derived status - **never** infer status
   from prose.
5. Fix `Cosulting`→`Consulting`. Append unnamed column 11 to the Castel Afrique comment. Store links
   verbatim, tracking params and all.
6. Print a report of everything ambiguous.
7. Run against `dev`.

Success criteria - parser unit tests:

- [x] Every date format above maps to the right 2026 date
- [x] The 7 expected extractions are found, and no others:
      SalesForce `22/jul - ajouté recruteuse...`; mc2i `27/july`, `30/july`, `06/aug`;
      Smoteo `30/07 premier appel...`; Alpine `11/08 appel avec sara turgeman...`;
      Swissquote `11 aug - update...`
- [x] Mid-sentence dates are **not** extracted - Keyteo's `entretien le 29/07` and Castel Afrique's
      `entretien le 29/07 avec Quentin` stay in the comment
- [x] `No après 1E` produces two updates, Interview before Rejected
- [x] A row whose comment is entirely unparseable keeps the comment intact and still gets its one
      guaranteed update

Success criteria - the actual run:

- [x] **26** applications, 0 rows skipped
- [x] Every application has at least one status update
- [x] **8** applications resolve to a closed status
- [x] Multi-line company `Morgan Phillips Recruitment. / Castel Afrique` survives as one string
- [x] `Alpine Consulting (client: SICPA)` is unsplit
- [x] No sector reads `Cosulting`
- [x] The Castel Afrique comment ends with the column-11 research text
- [x] The report explicitly flags ThoughtLabs (`I'm no longer interested` implies Withdrawn but is
      not inferred) and the two un-extracted mid-text dates
- [x] Re-running against a clean database produces identical counts

Phase 5 outcome, 2026-08-14:

- 26 applications, 35 status updates, 0 rows skipped. 35 = 26 guaranteed + 2 for the second half of
  the two `No après 1E` rows + 7 pulled out of comments. 40 parser tests.
- Extraction rule: a line counts only if it **starts** with a date that actually parses. That single
  rule gives exactly the 7 wanted entries and leaves Keyteo's and Castel Afrique's
  `entretien le 29/07` alone. `1 page resume` matches the shape of a date and is rejected because
  `page` is not a month, which is why parsing and matching are kept separate.
- MCA and Keyteo both resolve to `Rejected` even though their Interview and Rejected updates share a
  date. This is the `clock_timestamp()` change from Phase 4 paying off; under `now()` the pair would
  have tied and the current status would have been undefined.
- The report flags 6 things, of which 3 were not anticipated by the plan: `Dernière maj` on TheFork,
  Joko and sanofi is later than any date the sheet gives a reason for. The plan's guaranteed update
  is dated from `Candidaté le`, so those three lose the date of the event that closed them. Not
  guessed - reported.
- The unnamed eleventh column is keyed on `""`, not `None`: the header line ends in a bare comma, so
  `csv.DictReader` names the column rather than treating it as overflow.
- The script refuses to run against a non-empty database. Re-running after a truncate reproduces the
  same 26/35 exactly.

---

## Phase 6 - Front-end foundation

1. `create-next-app` with TypeScript, Tailwind, App Router. Add shadcn/ui.
2. `npm run gen:types` running `openapi-typescript ../backend/openapi.json`. Output committed, never
   hand-edited.
3. `lib/api.ts` - a thin server-only fetch wrapper attaching `X-API-Key` and `BACKEND_URL`. Marked
   `import 'server-only'` so a client import is a build error, not a leaked key.
4. Auth: `/login` Server Action comparing `APP_PASSWORD`, setting an HttpOnly `jose` HS256 cookie
   signed with `AUTH_SECRET`. Middleware redirects unauthenticated requests. **Confirm whether the
   installed Next version wants `middleware.ts` or `proxy.ts`** - it was renamed in Next 16.
5. Zinc base, indigo accent, working light and dark.

Success criteria:

- [x] `npm run gen:types` regenerates with no diff when the API is unchanged
- [x] Importing `lib/api.ts` from a client component fails the build
- [x] Wrong password does not set a cookie; right password redirects to the list
- [x] Hitting any route unauthenticated redirects to `/login`
- [x] The cookie is HttpOnly and invisible to `document.cookie`
- [x] A cookie tampered with fails signature validation and forces re-login
- [x] `BACKEND_API_KEY` appears nowhere in the client bundle: `grep -r` over `.next/static` is clean

Phase 6 outcome, 2026-08-14:

- Next 16.3.1, so the answer to the open question is **`proxy.ts` with a named `proxy` export**;
  `middleware.ts` is deprecated. There is a codemod, `npx @next/codemod middleware-to-proxy`.
- The `server-only` guarantee was proved by temporarily importing `lib/api.ts` into a client
  component: the build fails with `'server-only' cannot be imported from a Client Component module`.
  The probe was then removed.
- All three secrets were grepped for across the 31 files of `.next/static`: none appear.
- Session enforcement was probed over HTTP with a hand-minted token: no cookie, a tampered
  signature, and a garbage value all 307 to `/login`; a valid one gets 200 and bounces `/login`
  back to `/`. The password paths and the HttpOnly flag are covered by Playwright.
- The shadcn CLI has changed: there is no `--base-color`, it wants `-b radix -p nova`. The default
  preset is the newer "base" component library rather than Radix.
- `create-next-app` also generates `frontend/AGENTS.md` and `CLAUDE.md`, and `next dev` rewrites
  them. They are committed rather than fought with, per Next's own note inside them.

Phase 7 outcome, 2026-08-14:

- Verified against the imported data, not fixtures: the list renders 18 open and 26 with the toggle
  off, ordered most-recent-update first, and the one 4.5 rating shows as 4.5.
- Castel Afrique is the hard case and it holds up: the multi-line company renders across two lines,
  the tracking-laden LinkedIn URL wraps instead of stretching the page, and the column-11 research
  text sits at the end of the comment.
- Screenshotted at 1280 and 375 in both themes. Horizontal overflow at 375px measured as **0px** on
  both the list and the detail screen.
- Both list and timeline carry `aria-label`s, which is also what lets the E2E locators tell an
  application row apart from a timeline entry.

---

## Phase 7 - Screens

Built against the imported data, so every screen meets real messy content immediately.

1. **List** - title, company, sector, location, rating, current status badge, last update date.
   `Hide closed` toggle defaulting to on, as a URL search param so it survives refresh and is
   linkable.
2. **Detail** - all fields, link, full timeline reverse-chronologically. Add-update form.
3. **Create / edit** - one form. Create requires an initial status and date.
4. **Delete** - from detail, with confirmation.
5. Status badges per the AGENTS.md colour table, every badge carrying its text label.
6. `revalidatePath` after every mutation.

Success criteria:

- [x] List defaults to hiding closed; toggling reveals all 26
- [x] The toggle state survives a page refresh
- [x] Sorted most-recent-update first
- [x] Castel Afrique's multi-line comment and the long LinkedIn URLs do not break the layout
- [x] Adding an update changes the derived status on the list without a manual refresh
- [x] Ratings render half points; the one application with 4.5 shows 4.5
- [x] Every badge carries a text label - never colour alone
- [x] Legible in light and dark, verified by eye in both
- [x] Usable at 375px width
- [x] Deleting an application removes it and its timeline

---

## Phase 8 - End-to-end

Playwright against both services, seeded independently of the imported data.

Success criteria:

- [x] Login with the wrong then the right password
- [x] Create an application with its first status update; it appears in the list
- [x] Add a second update on a later date; the list's current status changes
- [x] Add a second update **on the same date**; the tiebreak resolves as the API tests say it does
- [x] `Hide closed` shows and hides correctly
- [x] Edit persists
- [x] Delete removes it
- [x] Suite passes from a cold start with one command
- [x] Suite leaves no residue - reruns cleanly without manual cleanup

Phase 8 outcome, 2026-08-14:

- 12 tests, `npm run test:e2e`, about 2.5 minutes. Run twice back to back with no cleanup in
  between; both green.
- Playwright starts both services itself on 3100 and 8100. It builds the frontend rather than using
  `next dev`: compile-on-demand made navigations time out at 60s and the suite took 5.8 minutes
  before failing. Port 3000 is also occupied on this machine by an unrelated project, and Next 16
  refuses to start a second dev server for the same directory, so fixed non-default ports avoid both
  problems.
- Setup and teardown projects empty the database through the API, so the suite depends on nothing
  and leaves nothing.
- Two real test bugs found and fixed, not product bugs: navigating straight after submitting the
  add-update form raced the server action, so `addUpdate` now waits for the timeline to grow; and
  `getByRole("alert")` matched Next's route announcer as well as the form error.

---

## Phase 9 - Deploy

1. Two Vercel projects from this repo: root `frontend/`, and root `backend/` on the Python runtime.
2. Env vars per the AGENTS.md tables. `DATABASE_URL` goes to the **backend only**. Backend gets the
   `main` branch pooled URL, not `dev`.
3. Deploy the backend first - the frontend needs its URL for `BACKEND_URL`.
4. `alembic upgrade head` against `main`.
5. Run the CSV import once against `main`.
6. Deploy the frontend.
7. Smoke test production.

Success criteria:

- [ ] Both projects deploy green
- [ ] Backend `/health` responds on its production URL
- [ ] Backend returns 401 to a request from outside with no API key - confirmed by curling it directly
- [ ] Production login works with the real `APP_PASSWORD`
- [ ] Production list shows 26 applications, 8 closed
- [ ] Create, edit, add-update, delete all work against production
- [ ] Cold start is tolerable - first request under ~3s
- [ ] No secret is readable in the client bundle or in any response header

---

## Phase 10 - Cleanup

1. Delete `scripts/import_csv.py` and its tests. It is a one-off, and the spec says so.
2. Keep `candidatures - jul.csv` as the archival record.
3. Fix by hand, in the UI, whatever the Phase 5 report flagged - starting with ThoughtLabs, which is
   almost certainly Withdrawn.
4. Final README pass: how to run both services, how to migrate, how to deploy.

Success criteria:

- [ ] Import script gone; `pytest` still green without it
- [ ] Flagged ambiguities resolved in the UI
- [ ] A fresh clone can be brought up from the README alone
- [ ] No emojis anywhere in the repo

---

## Risks

**Cold starts on a two-hop path.** Browser → Next → FastAPI, both serverless. The first request after
idle pays both. Expected, and fine for one user; if it grates, cache in the Next layer rather than
letting the browser reach FastAPI directly.

**`NullPool` is not optional.** Neon's pooled endpoint plus `NullPool` is the combination that works
in serverless. Any other pairing works locally and exhausts connections in production - which is why
Phase 9 smoke-tests against real production rather than trusting the local run.

**Generated types drift.** `openapi.json` is committed. Change Pydantic without regenerating and
TypeScript keeps compiling against a schema that no longer exists. Phase 6's no-diff check is what
catches it.

**Import is destructive if re-run.** It assumes an empty database. Running it twice doubles
everything. It runs exactly twice by design - once on `dev`, once on `main` - and is deleted after.
